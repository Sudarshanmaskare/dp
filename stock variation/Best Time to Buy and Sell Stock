class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int mini=INT_MAX;
        int ans=0;
        for(auto var:prices){
            mini=min(mini,var);
            ans=max(ans,var-mini);
        }
        return ans;
    }
    
};
